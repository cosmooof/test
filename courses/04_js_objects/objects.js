// Синтаксис
/*export default () => {
  const obj = {
    files: ['src/objects.js'],
    config: true,
  };
  return obj;
};*/

// Модификация
/*const normalize = (obj) => {
  obj.name = _.capitalize(obj.name);
  obj.description = obj.description.toLowerCase();
};*/


